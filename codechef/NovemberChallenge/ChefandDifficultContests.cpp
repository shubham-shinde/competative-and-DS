#include<bits/stdc++.h>
using namespace std;
#define FORI(s, n) for(int i=s; i<=n; i++)
typedef long long ll;

int main() {
    ll T;
    cin>>T;
    while(T--) {
        ll A, B;
        cin>>A>>B;
        cout<<"YES"<<endl;
    }
    return 0;
}